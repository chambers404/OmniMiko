<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme. Compose drives the real color scheme at runtime
         (see ui/theme/Theme.kt); this exists for the splash/launch window. -->
    <style name="Theme.OmniMiko" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@color/omnimiko_background</item>
        <item name="android:navigationBarColor">@color/omnimiko_background</item>
        <item name="android:windowBackground">@color/omnimiko_background</item>
    </style>
</resources>
